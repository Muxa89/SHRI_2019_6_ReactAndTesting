import * as React from 'react';
import * as FolderIcon from 'src/components/InfoTable/icons/Folder.svg';

const Folder = (): React.ReactElement => <img src={FolderIcon} alt={'folder'} />;

export default Folder;
