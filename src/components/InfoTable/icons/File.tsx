import * as React from 'react';
import * as FileIcon from 'src/components/InfoTable/icons/File.svg';

const File = (): React.ReactElement => <img src={FileIcon} alt={'file'} />;

export default File;
